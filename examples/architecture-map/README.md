# OV01 Architecture Card

This deterministic example describes an agent system across eight architecture dimensions and compares two designs.

```bash
python examples/architecture-map/architecture_card.py
```

It does not call a model. The purpose is to make architectural choices explicit before selecting a framework or implementing an agent loop.
