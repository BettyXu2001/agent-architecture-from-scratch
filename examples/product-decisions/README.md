# P01 Product Architecture Classifier

This deterministic example turns a few product requirement signals into a starting architecture recommendation.

Run it with:

```bash
python examples/product-decisions/agent_product_classifier.py
```

It deliberately does not call an LLM. The goal is to make product assumptions visible and discussable, not to automate the final architecture decision.
